package com;

class Enemy1State extends EnemyState {
    // var movingState:MovingState = LEFT;

    // override public function update(coms:game.ComStore, dt:Float) {
    //     super.update(dt);

    //     switch movingState {
    //         case LEFT: {
                
    //         }
    //     }
    // }
}